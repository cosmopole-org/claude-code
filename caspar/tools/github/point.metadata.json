{
  "isMcp": true,
  "kind": "tool",
  "tool_id": "github",
  "name": "github",
  "vm_name": "github-vm",
  "machine_id": "github-machine",
  "image_name": "github-tool:latest",
  "container_name": "tool-github",
  "category": "development",
  "description": "A full git + GitHub client bound to a Decillion space: connect a GitHub account (OAuth), then browse organizations and repositories and clone, branch, commit, push, pull, merge, open and merge pull requests, and manage issues.",
  "usecases": [
    "you need to look at or change code in a GitHub repository the space is connected to — read files, branches, commits, diffs",
    "you need to clone a repository into the space and work on it, then commit and push your changes",
    "you need to open, review or merge a pull request, or create/close issues",
    "you need to see an organization's repositories and their state (open PRs, default branch, permissions)"
  ],
  "howToTalk": "Signal it with `function` naming the operation. Connection: `status` (default), `orgs`, `disconnect`, `set_shared`. Read: `repos` (optionally `org`), `repo`, `branches`, `commits`, `pulls`, `get_pull`, `issues` — all take `repo` = \"owner/name\". Git on the space's shared sandbox machine: `clone`, `pull`, `push`, `commit` (needs `message`), `checkout` (`branch`, optional `create`), `merge` (`branch`), `git_status`, `git_log`, `read_file`/`write_file`/`delete_file`/`list_dir` (`path`), `list_cloned`. GitHub write: `create_pull` (`title`,`head`,`base`), `merge_pull` (`number`, optional `merge_method`), `update_pull`, `create_issue` (`title`). All filesystem/git work runs on the space's cloud sandbox (the same machine your other tools use), so clones are shared by the whole space. The space and the connected account are already bound — never pass a space id or a token.",
  "prompt": "Use this to actually work with the space's GitHub: read and change code, and drive git and GitHub. Name the operation with `function`. The space is connected to one GitHub account by a human member; you act as that account. Repositories are addressed as `repo`=\"owner/name\". All clones and file changes happen on the space's shared cloud sandbox (the same machine as the Files tool), so clone a repo before running git on it and everyone in the space sees the same files.",
  "request_point": "tool::github::request",
  "response_point": "tool::github::response",
  "risk_level": "high",
  "risk": "high",
  "requires_network": true,
  "requiresNetwork": true,
  "bindSpace": true,
  "autoAttach": false,
  "function": "status",
  "requiredArgs": ["function"],
  "categories": ["development", "execution"],
  "routes": { "development": "status" },
  "argSchema": {
    "function": { "type": "string", "description": "operation: status (default) | orgs | repos | repo | branches | commits | pulls | get_pull | issues | clone | pull | push | commit | checkout | merge | git_status | git_log | list_cloned | read_file | write_file | delete_file | list_dir | create_pull | merge_pull | update_pull | create_issue | disconnect | set_shared" },
    "repo": { "type": "string", "description": "target repository as \"owner/name\" (e.g. \"acme/website\")" },
    "org": { "type": "string", "description": "for repos: limit to one organization's repositories" },
    "branch": { "type": "string", "description": "a branch name, for checkout/push/pull/merge/commits" },
    "create": { "type": "bool", "description": "for checkout: create the branch" },
    "message": { "type": "string", "description": "for commit: the commit message" },
    "path": { "type": "string", "description": "for read_file/write_file/list_dir: a path inside the cloned repo" },
    "content": { "type": "string", "description": "for write_file: the file content" },
    "title": { "type": "string", "description": "for create_pull/create_issue: the title" },
    "head": { "type": "string", "description": "for create_pull: the source branch" },
    "base": { "type": "string", "description": "for create_pull: the target branch" },
    "number": { "type": "number", "description": "for get_pull/merge_pull/update_pull: the pull request number" },
    "merge_method": { "type": "string", "description": "for merge_pull: merge | squash | rebase" }
  },
  "tools": [
    {
      "name": "status",
      "desc": "report whether the space is connected to GitHub, as which account, whether the connection is shared, and whether the caller may use/manage it",
      "args": {}
    },
    {
      "name": "orgs",
      "desc": "list the connected user and the organizations they granted access to",
      "args": {}
    },
    {
      "name": "repos",
      "desc": "list repositories the connection can see; pass `org` to scope to one organization",
      "args": { "org": { "type": "STRING", "desc": "optional organization login to scope to" } }
    },
    {
      "name": "repo",
      "desc": "details for one repository (default branch, permissions, description, counts)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" } }
    },
    {
      "name": "branches",
      "desc": "list a repository's branches with their head SHA and protection",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" } }
    },
    {
      "name": "commits",
      "desc": "recent commits on a repository (optionally a `branch` or `path`)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "optional branch" } }
    },
    {
      "name": "pulls",
      "desc": "list pull requests (default state open)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "state": { "type": "STRING", "desc": "open | closed | all" } }
    },
    {
      "name": "get_pull",
      "desc": "one pull request with its mergeability",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "number": { "type": "NUMBER", "desc": "PR number" } }
    },
    {
      "name": "create_pull",
      "desc": "open a pull request",
      "args": {
        "repo": { "type": "STRING", "desc": "owner/name" },
        "title": { "type": "STRING", "desc": "PR title" },
        "head": { "type": "STRING", "desc": "source branch" },
        "base": { "type": "STRING", "desc": "target branch" },
        "body": { "type": "STRING", "desc": "optional description" }
      }
    },
    {
      "name": "merge_pull",
      "desc": "merge a pull request",
      "args": {
        "repo": { "type": "STRING", "desc": "owner/name" },
        "number": { "type": "NUMBER", "desc": "PR number" },
        "merge_method": { "type": "STRING", "desc": "merge (default) | squash | rebase" }
      }
    },
    {
      "name": "update_pull",
      "desc": "edit a pull request (title/body/state); set state to closed to close it",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "number": { "type": "NUMBER", "desc": "PR number" } }
    },
    {
      "name": "issues",
      "desc": "list issues (default state open)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "state": { "type": "STRING", "desc": "open | closed | all" } }
    },
    {
      "name": "create_issue",
      "desc": "open an issue",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "title": { "type": "STRING", "desc": "issue title" }, "body": { "type": "STRING", "desc": "optional body" } }
    },
    {
      "name": "clone",
      "desc": "clone a repository into the space (idempotent — fetches when already cloned)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "optional branch to check out" } }
    },
    {
      "name": "pull",
      "desc": "git pull the cloned repository from origin",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "optional branch" } }
    },
    {
      "name": "commit",
      "desc": "stage and commit changes in the cloned repository",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "message": { "type": "STRING", "desc": "commit message" }, "files": { "type": "ARRAY", "desc": "optional file list (default: all changes)" } }
    },
    {
      "name": "push",
      "desc": "git push the cloned repository to origin",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "optional branch (default HEAD)" } }
    },
    {
      "name": "checkout",
      "desc": "switch branches (or create one with `create`)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "branch name" }, "create": { "type": "BOOL", "desc": "create the branch" } }
    },
    {
      "name": "merge",
      "desc": "git merge a branch into the current one in the clone",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "branch": { "type": "STRING", "desc": "branch to merge in" } }
    },
    {
      "name": "git_status",
      "desc": "porcelain status + current branch of the cloned repository",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" } }
    },
    {
      "name": "git_log",
      "desc": "recent local commits of the cloned repository",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "limit": { "type": "NUMBER", "desc": "how many (default 20)" } }
    },
    {
      "name": "read_file",
      "desc": "read a file out of a cloned repository",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "path": { "type": "STRING", "desc": "path inside the repo" } }
    },
    {
      "name": "write_file",
      "desc": "create or overwrite a file in a cloned repository (on the space's sandbox)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "path": { "type": "STRING", "desc": "path inside the repo" }, "content": { "type": "STRING", "desc": "file content" } }
    },
    {
      "name": "delete_file",
      "desc": "delete a file or directory in a cloned repository (on the space's sandbox)",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "path": { "type": "STRING", "desc": "path inside the repo" } }
    },
    {
      "name": "list_dir",
      "desc": "list a directory inside a cloned repository as structured {name,type,size} rows",
      "args": { "repo": { "type": "STRING", "desc": "owner/name" }, "path": { "type": "STRING", "desc": "directory (default repo root)" } }
    },
    {
      "name": "list_cloned",
      "desc": "list the repositories cloned into this space",
      "args": {}
    },
    {
      "name": "oauth_start",
      "desc": "begin the GitHub OAuth web flow (front-end only); returns the authorize URL to open in a browser tab",
      "args": {}
    },
    {
      "name": "oauth_wait",
      "desc": "long-poll until the OAuth callback stores the token (front-end only); returns connected/pending/expired",
      "args": {}
    },
    {
      "name": "set_shared",
      "desc": "owner-only: toggle whether the rest of the space (people and agents) may use the connection",
      "args": { "shared": { "type": "BOOL", "desc": "true to share with the space, false to keep private to the connecting member" } }
    },
    {
      "name": "disconnect",
      "desc": "owner-only: remove the stored GitHub token and connection for the space",
      "args": {}
    }
  ]
}
